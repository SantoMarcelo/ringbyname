---
default: -p dev
local: -p json -p sandbox
ci: -p json -p sandbox -p headless
json: --format progress --format json --out=log/report.json
dev: AMBIENTE=dev
sandbox: AMBIENTE=sandbox
headless: BROWSER=headless
firefox: BROWSER=firefox